正则表达式概念：

正则表达式（Regular Expression，简称 Regex 或 RegExp）是一种用来匹配字符串中字符组合的模式。
正则表达式提供了一种灵活且强大的方式来查找、替换、验证和提取文本数据。

在学习 爬虫 我们可以利用正则表达式提取网页中的部分数据-数据解析

元字符

\d 匹配数字 等价于 [0-9]

\w 匹配数字字母下划线

\s 匹配任意空白字符

大写的即为非 。。。

量词元字符

* 匹配前面的子表达式零次或多次  示例：ab*c 匹配 "ac", "abc", "abbc"
+ (加号) 匹配前面的子表达式一次或多次
? 匹配前面的子表达式零次或一次 示例：colou?r 匹配 "color" 和 "colour"
{n} 精准匹配前面字符多少次  a{3} 匹配：aaa
{n,} 至少匹配 n 次 a{2,} aa  aaa
[]  匹配[]中任意一个字符
^ 字符串必须以……开头
$ 字符串必须以……结尾

贪婪量词
.*?：零次或多次，但尽可能少
.*：零次或多次，但尽可能多

json 数据的处理
json.dumps() 将 json 数据转换为 字符串返回
json.dump() 将 json 数据转换为 字符串 并存储到文件中